const style = {
    stopBtn: {
        background: "darkred",
        color: "white"
    }
}

const StopBtn = () => {
    return <button style = { style.stopBtn }>Stop</button>
}

export default StopBtn